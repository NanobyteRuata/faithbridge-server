// For permissions
export enum RESOURCE_ACTIONS {
  CREATE = 'CREATE',
  READ = 'READ',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
}

// For logging
export enum NON_RESOURCE_ACTIONS {
  LOGIN = 'LOGIN',
}
